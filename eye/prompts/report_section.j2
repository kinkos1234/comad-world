{# 리포트 섹션 서술 생성 프롬프트 템플릿 #}
아래 분석 데이터를 바탕으로 보고서 섹션 본문을 마크다운으로 작성하세요.

## 섹션 정보

- 제목: {{ section_title }}
- 분석공간: {{ source_spaces | join(", ") }}

## 관련 핵심 발견

{% for finding in key_findings %}
- [Rank {{ finding.rank }}] {{ finding.finding }} (신뢰도 {{ "%.0f"|format(finding.confidence * 100) }}%)
{% endfor %}

## 분석 데이터

{{ analysis_data }}

## 인터뷰 후보 엔티티

{% for entity in interview_entities %}
- **{{ entity.name }}** ({{ entity.object_type }})
  - stance: {{ "%.2f"|format(entity.stance) }} → 어조: {{ entity.tone }}
  - influence: {{ "%.2f"|format(entity.influence_score) }} → 강도: {{ entity.intensity }}
  - 주요 관계: {{ entity.relationships | join(", ") | default("없음") }}
{% endfor %}

## 출력 형식 (반드시 이 순서대로)

```
### 핵심 요약

(1~2문장 요약)

### 상세 분석

(3~5단락. 수치는 **볼드**로, 인과관계는 A → B → C로)

### 주요 인과 경로

- 출발점 → 중간 → 도착점 (영향: 설명)
- ...

### 이해관계자 시각

> "인용문" — **엔티티명**, 역할

> "인용문" — **엔티티명**, 역할
```

## 주의: JSON 필드명 노출 금지, 메타 서술 금지, 자연스러운 한국어로 서술